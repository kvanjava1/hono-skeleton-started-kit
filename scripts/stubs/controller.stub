import type { Context } from 'hono';
import { successResponse } from '../utils/response.util.ts';

/**
 * {{NAME}} Controller
 */
export const get{{NAME}}s = async (c: Context) => {
  return successResponse(c, '{{NAME}}s fetched');
};
