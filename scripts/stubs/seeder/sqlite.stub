import { getSqliteDb } from '../../../src/database/sqlite.connection.ts';
import { logger } from '../../../src/utils/logger.util.ts';
import { BCRYPT_SALT_ROUNDS } from '../../../src/configs/constants.ts';

export const seed = async (): Promise<void> => {
  logger.info('Running SQLite seeder: {{NAME}}');
  const db = getSqliteDb('sqlite1');

  // Add your seeding logic here
  // db.run('INSERT INTO ...');
};
